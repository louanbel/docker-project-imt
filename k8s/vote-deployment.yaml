apiVersion: apps/v1
kind: Deployment
metadata:
  name: vote
spec:
  replicas: 3
  template:
    metadata:
      name: vote
      labels:
        app: vote
    spec:
      containers:
        - image: europe-west9-docker.pkg.dev/geometric-flux-421107/voting-images/vote
          name: vote
  selector:
    matchLabels:
      app: vote
